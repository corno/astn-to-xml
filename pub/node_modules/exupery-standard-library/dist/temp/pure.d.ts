import * as _et from 'exupery-core-types';
export type Temp_Pure = {
    'dictionary': {
        'build, overwrite clashing keys': <T>($: ($c: {
            'add entry': (key: string, value: T) => void;
        }) => void) => _et.Dictionary<T>;
        'build, ignore clashing keys': <T>($: ($c: {
            'add entry': (key: string, value: T) => void;
        }) => void) => _et.Dictionary<T>;
    };
};
export declare const $$: Temp_Pure;
