import * as _et from 'exupery-core-types';
export type Key_Value_Pair<T> = {
    'key': string;
    'value': T;
};
export type Compare_Result = 'left is before right' | 'both are equal' | 'right is before left';
export declare const $$: <T>($: _et.Dictionary<T>, $c: {
    "compare": (left: string, right: string) => Compare_Result;
}) => _et.Array<Key_Value_Pair<T>>;
