import { _Guaranteed_Query } from "../../types/Guaranteed_Query";
type Executer<T> = {
    'execute': (on_value: ($: T) => void) => void;
};
/**
 * returns an {@link Async_Value }
 * @param executer the function that produces the eventual value
 * @returns
 */
export declare function __create_guaranteed_query<T>(executer: Executer<T>): _Guaranteed_Query<T>;
export {};
